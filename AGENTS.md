# polyglot-debug — agent notes

- Upstream-shaped patches: LLDB ObjectFile chord scoring; GDB/BFD survey instead of greedy first-match.
- Not a fork of llvm-project. Apply `patches/` onto a clone, or use `plugin/ObjectFilePolyglot.cpp` as the intended in-tree file.
- Experimental. File issues; do not claim merged upstream.
